var React = require('react');
var Router = require('react-router');

var SearchStats = React.createClass({
    render: function () {
        return (
            <div>Selecteaza un membru din lista pentru a afla mai multe informatii.</div>
        );
    }
});

module.exports = SearchStats;
